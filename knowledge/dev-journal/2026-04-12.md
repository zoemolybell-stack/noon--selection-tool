# 2026-04-12

## 21:32 上下文同步
### 做了什么
- 在 `D:/claude noon v1` 运行 `python .\scripts\sync_obsidian_context.py`，刷新了 Obsidian 动态知识库页与 `tmp/obsidian_context_snapshot.json`。
- 读取并核对了 `D:/claude noon v1/tmp/obsidian_context_snapshot.json`、`90-当前开发上下文.md`、`91-近期开发节点.md`，以及上一条开发日记 `2026-04-11.md`。
- 发现自上次记录后仍有一批实质性源码变化落在 `keyword_runtime_contract.py`、`ops/crawler_control.py`、`run_task_scheduler.py`、`web_beta/app.py` 及其测试，但脚本生成的 `91` 尚未覆盖这批节点，因此手工补写了 `90/91` 与今日日志。
- 本轮新增开发节点主要包括：
- 统一 runtime 结果文件失败契约，补齐 `failure_details.snapshot_id`、`expected_result_file` 与 `result_contract_mismatch` 判定，让 noon/amazon 的缺失结果文件、不完整 JSON、结构不匹配都能落到同一套诊断语义。
- crawl plan 调度下沉到 round / round item：`category_ready_scan` 按类目拆分，`keyword_batch` 按关键词拆分，`keyword_monitor` 可按 baseline seed 逐项派发，并把 `snapshot` / `output_dir` 绑定到 item 级上下文。
- `category_ready_scan` 取消重复 follow-up warehouse sync，避免 ERP 已经完成增量持久化后还被额外全量同步拖长。
- worker 执行链路补上 lease keepalive、任务取消/丢租约后的进程树终止，以及重复锁冲突的统一 skip 语义。
- 运行中心 `runs summary` 现在会合并 ops store 中真实活跃的 keyword task，减少只看 `keyword_runs_log` 时的活跃态漏算。

### 为什么
- 相比 2026-04-11 记录的 runtime truth / idle overlay / NAS 稳定版推进，这一轮变化已经把“结果真值如何定义”“计划如何拆成可调度粒度”“worker 如何在取消或租约丢失时收敛”“运行中心如何看到真实活跃任务”继续往下打通，属于后续恢复开发上下文必须保留的执行链路信息。
- 如果只保留脚本自动刷新的页，不把这批 4 月 12 日凌晨的手工判断补进日记和 `91`，后续回看会误以为上下文仍停留在 4 月 11 日的治理节点。

### 验证结果
- 同步脚本执行成功，`tmp/obsidian_context_snapshot.json` 更新时间为 `2026-04-12 21:30:44`。
- 执行 `python -m unittest tests.test_keyword_runtime_contract tests.test_web_phase0 tests.test_run_task_scheduler`，结果为 `Ran 19 tests in 7.777s` 且 `OK`。
- `91-近期开发节点.md` 已新增 2026-04-12 的手工节点。

### 风险
- `sync_obsidian_context.py` 当前没有自动吸收这批 4 月 12 日凌晨的源码节点，说明“脚本快照”和“人工上下文判断”之间仍有漏记风险。
- `00-07` 架构专题页本轮没有手工更新；如果后续 round dispatch、worker lifecycle 或 runtime contract 继续固化成稳定边界，需要再决定是否上升为架构专题。
- 终端直接读取 Obsidian 中文页面仍有编码噪音，跨工具核对时仍应优先依赖人工日记与明确的验证命令结果。

### 下一步
- 下次同步时优先检查 `sync_obsidian_context.py` 是否已经能够自动识别这类 round dispatch / runtime contract 节点，减少人工补录。
- 持续关注 round/item 调度在真实 NAS 运行中的表现，尤其是 monitor seed 拆分、重复锁跳过和取消/丢租约后的 worker 清理是否稳定。
- 如果运行中心与调度链路继续围绕 round dispatch 扩展，再评估是否需要把结论沉淀进 `05-调度、任务系统与运行中心.md` 等稳定专题页。
